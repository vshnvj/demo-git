name: Adhoc - Build Images All
run-name: Build Images All
on:
  workflow_dispatch:
  
jobs:
  ckb-users:
    if: "github.event.inputs.option == 'ckb-users' || github.event.inputs.option == 'Both'"
    steps:
      - run : |
          echo "ckb-users"  

  
  ckb-planograms:
    if: github.event.inputs.option == 'ckb-planograms'
    steps:
      - run : |
          echo "ckb-planograms"  
